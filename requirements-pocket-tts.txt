# Optional local TTS profile. ElevenLabs remains the default provider, so the
# heavyweight CPU model stack is deliberately not forced on default installs.
# The PyTorch CPU index avoids several gigabytes of unnecessary CUDA wheels on
# Linux, matching Pocket TTS's own CPU-only installation guidance.
--extra-index-url https://download.pytorch.org/whl/cpu
-r requirements.txt
pocket-tts==3.1.0
# audioop was removed from CPython 3.13. Pocket's in-provider PCM->mu-law path
# and the existing Bluetooth codec both still use that stdlib-compatible API.
# audioop-lts provides the same import name only on Python 3.13+.
audioop-lts==0.2.2; python_version >= "3.13"
