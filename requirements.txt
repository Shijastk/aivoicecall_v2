fastapi>=0.109.0
uvicorn>=0.27.0
websockets>=12.0
httpx>=0.27.0
python-multipart>=0.0.9
twilio>=9.0.0
numpy>=1.24.0
python-dotenv>=1.0.0
openai>=1.0.0
# 7.x made `agent_rest` a required field on DeepgramClientEnvironment;
# shuo/services/flux.py passes it. Deleted in Phase 3 (Sarvam replaces Flux).
deepgram-sdk>=7.0.0
elevenlabs>=1.0.0
matplotlib>=3.7.0
pytest>=7.0.0
pytest-asyncio>=0.21.0
# audioop-lts>=0.2.1  # Only needed for Python 3.13+ (audioop is built-in for earlier versions)
