diff --git a/shuo/services/llm.py b/shuo/services/llm.py
--- a/shuo/services/llm.py
+++ b/shuo/services/llm.py
@@ -328,8 +328,15 @@ class LLMService:
                 )
             raise
         except Exception as e:
-            log.error("Prepared generation failed", e)
-            await self._on_done()
+            if self._running and not assistant_response:
+                log.info(
+                    "LLMRequest: prepared_reuse_failed_before_output; "
+                    "falling_back_to_normal"
+                )
+                await prepared.cancel()
+                await self._generate()
+                return
+            else:
+                log.error("Prepared generation failed", e)
+                await self._on_done()
         finally:
             await prepared.cancel()
             self._running = False
diff --git a/shuo/bluetooth/speculative.py b/shuo/bluetooth/speculative.py
--- a/shuo/bluetooth/speculative.py
+++ b/shuo/bluetooth/speculative.py
@@ -23,6 +23,7 @@ class PreparedResponse(Protocol):
     @property
     def launched(self) -> bool: ...
+    def start(self) -> None: ...
     async def wait_first_token(self) -> float: ...
@@ -148,6 +149,7 @@ class SpeculativeTurnCoordinator:
         generation_id, committed_transcript, prepared = committed
         if transcript.strip() != committed_transcript:
+            self._discard_committed("take_mismatch")
             return None
         self._committed = None
diff --git a/tests/test_bluetooth_phase4c.py b/tests/test_bluetooth_phase4c.py
--- a/tests/test_bluetooth_phase4c.py
+++ b/tests/test_bluetooth_phase4c.py
@@ -82,8 +82,11 @@ async def test_prepared_stream_prefetches_only_first_content_token_then_reuses_rest():
     assert stream.reads == 2
     assert stream.closed is False
 
+    async def collect(token):
+        tokens.append(token)
+
     tokens = []
-    await prepared.consume(tokens.append)
+    await prepared.consume(collect)
@@ -205,8 +208,11 @@ async def test_coordinator_promotes_matching_inflight_stream_exactly_once():
 
     stream.release.set()
+    async def collect(token):
+        tokens.append(token)
+
     tokens = []
-    await prepared.consume(tokens.append)
+    await prepared.consume(collect)
     assert tokens == ["first", " rest"]
